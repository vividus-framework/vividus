project.description = 'VIVIDUS MCP Server'

dependencies {
    implementation project(':vividus')
    implementation project(':vividus-util')

    implementation platform(group: 'io.modelcontextprotocol.sdk', name: 'mcp-bom', version: '0.13.0')
    implementation(group: 'io.modelcontextprotocol.sdk', name: 'mcp-core')
    implementation(group: 'io.modelcontextprotocol.sdk', name: 'mcp-json-jackson2')

    implementation platform(group: 'org.apache.logging.log4j', name: 'log4j-bom', version: '2.25.2')
    implementation(group: 'org.apache.logging.log4j', name: 'log4j-core')

    testImplementation platform(group: 'org.junit', name: 'junit-bom', version: '5.13.4')
    testImplementation(group: 'org.junit.jupiter', name: 'junit-jupiter')
    testRuntimeOnly('org.junit.platform:junit-platform-launcher')
    testImplementation platform(group: 'org.mockito', name: 'mockito-bom', version: '5.20.0')
    testImplementation(group: 'org.mockito', name: 'mockito-junit-jupiter')
}

project.description = 'VIVIDUS plugin for Azure Event Hub'

dependencies {
    api project(':vividus-engine')
    implementation project(':vividus-extension-azure')
    implementation('com.azure.resourcemanager:azure-resourcemanager-eventhubs:2.53.5')
    implementation platform(libs.slf4j.bom)
    implementation(libs.slf4j.api)

    testImplementation platform(libs.junit.bom)
    testImplementation(libs.junit.jupiter)
    testRuntimeOnly(libs.junit.platformLauncher)
    testImplementation platform(libs.mockito.bom)
    testImplementation(libs.mockito.junitJupiter)
    testImplementation(libs.slf4jTest)
}

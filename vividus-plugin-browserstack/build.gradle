project.description = 'VIVIDUS plugin for BrowserStack'

dependencies {
    implementation project(':vividus-engine')
    implementation project(':vividus-util')
    implementation project(':vividus-reporter')
    implementation project(':vividus-extension-selenium')
    implementation project(':vividus-plugin-json')

    runtimeOnly project(':vividus-plugin-rest-api')

    implementation(group: 'org.slf4j', name: 'slf4j-api', version: versions.slf4j)
    implementation(group: 'com.browserstack', name: 'automate-client-java', version: '0.12')
    implementation(group: 'com.browserstack', name: 'browserstack-local-java', version: '1.1.0')

    testImplementation platform(group: 'org.junit', name: 'junit-bom', version: '5.10.0')
    testImplementation(group: 'org.junit.jupiter', name: 'junit-jupiter')
    testImplementation platform(group: 'org.mockito', name: 'mockito-bom', version: '5.4.0')
    testImplementation(group: 'org.mockito', name: 'mockito-junit-jupiter')
    testImplementation(group: 'com.github.valfirst', name: 'slf4j-test', version: '3.0.1')
}
